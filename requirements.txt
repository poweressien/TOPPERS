# ── Core Django ───────────────────────────────────────────────
Django==4.2.13
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1

# ── Auth & OAuth ──────────────────────────────────────────────
django-allauth==0.63.3
dj-rest-auth==6.0.0

# ── CORS & Environment ────────────────────────────────────────
django-cors-headers==4.3.1
django-environ==0.11.2
python-decouple==3.8

# ── Database ──────────────────────────────────────────────────
# PythonAnywhere free tier = MySQL → use pymysql
# Paid tier / local with PostgreSQL → use psycopg2-binary
pymysql==1.1.1
dj-database-url==2.1.0

# ── Media & Images ────────────────────────────────────────────
Pillow==10.3.0

# ── Filtering ─────────────────────────────────────────────────
django-filter==24.2

# ── Real-time (channels — works without Redis on free tier) ───
channels==4.1.0

# ── Static Files ──────────────────────────────────────────────
whitenoise==6.7.0

# ── HTTP Requests (Paystack, VTPass) ──────────────────────────
requests==2.32.3

# ── Utilities ─────────────────────────────────────────────────
shortuuid==1.0.13

# ── Production server ─────────────────────────────────────────
gunicorn==22.0.0

# ── Optional: Celery (background tasks — not needed on free tier) ──
# Uncomment when upgrading to paid tier with Redis:
# celery==5.4.0
# redis==5.0.6
# channels-redis==4.2.0
# django-celery-beat==2.6.0
# django-celery-results==2.5.1
# daphne==4.1.2
